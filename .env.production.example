# ── Production Environment ────────────────────────────────────────
# These are SET IN RENDER DASHBOARD — do NOT commit real values here
# This file is safe to commit (no secrets).

NODE_ENV=production
PORT=10000

# MongoDB Atlas — replace <password> with your actual password
MONGODB_URI=mongodb+srv://abithamizha_db_user:<password>@cricket.x6pepse.mongodb.net/cricket_app?retryWrites=true&w=majority&appName=cricket

# JWT (set in Render dashboard)
JWT_SECRET=<your-strong-random-secret>
JWT_EXPIRES_IN=7d

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
